variable "create_s3_folder" {
  type    = list(string)
  default = []
}

resource "aws_s3_object" "folders" {
  for_each = toset(var.create_s3_folder)

  bucket = var.s3_bucket_id
  key    = "${trimsuffix(each.value, "/")}/"
}
